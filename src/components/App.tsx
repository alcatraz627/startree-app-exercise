import React, { useState } from "react";

import styles from "./app.scss";

const App = () => {
    return (
        <div className={styles.root}>
            <h3>Test project setup</h3>
        </div>
    );
};

export default App;
